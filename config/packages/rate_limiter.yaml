framework:
    rate_limiter:
        password_reset:
            policy: sliding_window
            limit: 3
            interval: '1 hour'
        registration:
            policy: sliding_window
            limit: 5
            interval: '1 hour'
